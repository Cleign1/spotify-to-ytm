[project]
name = "spotify-to-ytm"
version = "2.0.0"
description = "Import Spotify CSV playlists into YouTube Music"
dependencies = [
    "pandas",
    "ytmusicapi"
]

[project.scripts]
spotify-to-ytm "spotify-to-ytm.cli:main"

[tool.setuptools.packages.find]
include = ["spotify-to-ytm*"]